# دليل استخدام مستشارك الطبي الذكي
**الموقع:** https://smartmedcon.com
**اللغات:** العربية / English (زر التبديل في شريط التنقل العلوي)

---

# الجزء الأول — دليل المستخدم العادي (المريض)

## ١. إنشاء حساب جديد
١. افتح https://smartmedcon.com واضغط **Register / تسجيل** (أعلى اليمين).
٢. أدخل: الاسم الكامل، البريد الإلكتروني، اسم المستخدم، كلمة المرور.
٣. اضغط زر التسجيل — تدخل حسابك مباشرة.
٤. كل حساب جديد يحصل على **استشارة ذكاء اصطناعي مجانية واحدة**.

## ٢. تسجيل الدخول والخروج
- اضغط **Sign In / تسجيل الدخول** وأدخل اسم المستخدم وكلمة المرور.
- نسيت كلمة المرور؟ اضغط **Forgot password? / نسيت كلمة المرور** — يصلك رابط إعادة التعيين على بريدك الإلكتروني.
- للخروج: افتح قائمة صورتك الشخصية (أعلى اليمين) واختر تسجيل الخروج.

## ٣. تقديم استشارة طبية
١. من الصفحة الرئيسية أو لوحة التحكم اضغط **Submit for AI Analysis / أرسل للتحليل الذكي**.
٢. أدخل معلوماتك:
   - **الأعراض** — اكتب ما تشعر به (أو سجّل صوتياً — يُحوَّل الصوت إلى نص تلقائياً).
   - **التقارير الطبية** — ارفع نتائج المختبر، صور الأشعة، الوصفات (صور أو PDF).
   - **التاريخ المرضي** — الأمراض المزمنة، الأدوية، الحساسية.
٣. أرسل الطلب. يقوم الذكاء الاصطناعي بتحليل كل المعلومات وإصدار تقرير شامل.
٤. تتم مراجعة التقارير من مختصين طبيين قبل اعتمادها النهائي.

## ٤. فاحص الأعراض
- من القائمة → **Symptom Checker / فاحص الأعراض**: أداة سريعة موجَّهة لتقييم أعراضك قبل طلب استشارة كاملة.

## ٥. لوحة التحكم الخاصة بك
- **Dashboard / لوحة التحكم** (تظهر بعد تسجيل الدخول) تعرض **استشاراتي** مع حالة كل استشارة:
  - *Submitted / مُرسلة* → استُلمت وبانتظار المعالجة
  - *Completed / مكتملة* → التقرير جاهز — اضغط عليها لقراءة التحليل الكامل
  - *Cancelled / ملغاة*
- اضغط على أي استشارة لفتح صفحة التفاصيل (التقرير الكامل، الملفات، درجة الأولوية).

## ٦. رصيد الاستشارات والدفع
- الخطة المجانية: استشارة واحدة مجانية.
- بعد استهلاكها يمكنك شراء استشارات إضافية — بعد الدفع تصل إلى صفحة **تأكيد الدفع** ويتحدَّث رصيدك تلقائياً.
- رصيدك الحالي يظهر في ملفك الشخصي / لوحة التحكم.

## ٧. ملفك الشخصي
- **My Profile / ملفي الشخصي**: عدّل الاسم، النبذة، الصورة الشخصية؛ واطّلع على نوع اشتراكك وعدد الاستشارات المتبقية.

## ٨. أقسام المحتوى (بدون تسجيل دخول)
- **Blog / المدونة** — مقالات طبية
- **Videos / الفيديوهات** — فيديوهات طبية توضيحية
- **Podcasts / البودكاست** — حلقات صوتية
- **Contact / اتصل بنا** — أرسل رسالة للفريق

## ٩. ملاحظات مهمة
- عند أول استخدام يجب الموافقة على **إخلاء المسؤولية الطبية** — الخدمة تقدم تحليلاً تثقيفياً وليست تشخيصاً طبياً؛ راجع طبيبك دائماً.
- بياناتك محمية: كلمات المرور مشفَّرة بتقنية bcrypt وكل الاتصالات عبر HTTPS.

---

# الجزء الثاني — دليل مدير النظام (الأدمن)

**حساب الأدمن:** `jalalkhashman1974` (الدور: admin)
**لوحة الإدارة:** سجّل الدخول → افتح **/admin** (يظهر الرابط في قائمتك عند الدخول كأدمن)

## ١. لوحة الإدارة — تبويب الاستشارات
قائمة العمل الرئيسية. لكل استشارة يمكنك:
- **البحث** باسم المريض أو كلمة مفتاحية.
- **التصفية حسب الحالة:** الكل / مُرسلة / مكتملة / ملغاة.
- **التصفية حسب الأولوية:** 🔴 حرجة / 🟠 عاجلة / 🟡 متوسطة / 🔵 روتينية.
- **الترتيب:** الأحدث أولاً / الأقدم أولاً.
- **فتح الاستشارة** لـ: مراجعة تقرير الذكاء الاصطناعي، تعديله واعتماده، تغيير الحالة (مثلاً وضعها "مكتملة")، و**تصدير التقرير PDF**.
- العناصر الجديدة غير المقروءة تظهر بـ**شارة تنبيه** على رابط الإدارة في القائمة.

سير العمل: الطلبات الجديدة تصل بحالة *مُرسلة* → راجع تقرير الذكاء الاصطناعي → عدّل إن لزم → ضعها *مكتملة* → يرى المريض التقرير النهائي في لوحته.

## ٢. لوحة الإدارة — تبويب المستخدمين
- عرض جميع المستخدمين المسجلين: الاسم، البريد، الدور، نوع الاشتراك، الاستشارات المتبقية.
- إدارة الحسابات وأرصدة الاستشارات.

## ٣. لوحة الإدارة — تبويب الوسائط
- إضافة وإدارة **الفيديوهات** و**البودكاست** المعروضة في الموقع العام (اختيار النوع: فيديو / بودكاست).

## ٤. إدارة المدونة
- صفحة **Blog Management** منفصلة: كتابة وتحرير ونشر المقالات (عربي + إنجليزي).

## ٥. المراقبة والسجلات
- **Monitoring Dashboard** — نظرة عامة على صحة الخدمة.
- **Admin Report Log** — سجل التقارير المُصدَّرة.

## ٦. البنية التحتية (لمالك الموقع)
| المكوّن | المكان | ملاحظات |
|---|---|---|
| الموقع (التطبيق) | Hostinger Web App — نشر تلقائي من GitHub `smart-medical-consultant-b-version` (فرع `main`) | أي push إلى GitHub = إعادة نشر تلقائية |
| النطاق + SSL | smartmedcon.com (Hostinger) | شهادة SSL تُدار تلقائياً |
| قاعدة البيانات | MySQL 8.4 داخل Docker على السيرفر الخاص VPS (srv926545 — 31.97.126.199)، الحاوية `smc-mysql` | تعمل ٢٤/٧؛ الدخول الإداري عبر hPanel → VPS → Terminal ثم: `docker exec -it smc-mysql mysql -uroot -p` |
| تحليل الذكاء الاصطناعي | OpenRouter (Gemini 2.5 Flash) | المفتاح في متغيرات بيئة الـ Web App |
| أفاتار المقابلة | **الوضع الصوتي فقط (الخيار الحالي)** — صورة طبيب العيادة + صوت المتصفح. مجاني | الأفاتار المرئي المدفوع (LiveAvatar/HeyGen) مبنيّ لكنه مُعطّل. انظر §٧ |
| تحويل الصوت إلى نص | Groq Whisper | المفتاح في متغيرات البيئة |
| تخزين الملفات | Cloudflare R2 (حاوية `smc-files`) | |
| البريد (استعادة كلمة المرور) | Resend | |
| النسخ الاحتياطي | Aiven MySQL (لقطة مجمّدة، خطة مجانية تنام تلقائياً) + لقطة أسبوعية للـ VPS من Hostinger | |

**إذا فقد الموقع الاتصال بقاعدة البيانات:** السبب الأرجح تغيّر عنوان IP الصادر من الـ Web App. افتح hPanel → VPS → Terminal ونفّذ:
```
docker exec -it smc-mysql mysql -uroot -p
```
ثم أعد إنشاء مستخدم التطبيق بالعنوان الجديد (`CREATE USER 'smc_user'@'<new-ip>' ...` مع الصلاحيات) بما يطابق DATABASE_URL في متغيرات بيئة الـ Web App.

## ٧. الدخول عبر Google

يستطيع المريض التسجيل والدخول بحساب **Gmail / Google** بدل إنشاء اسم مستخدم
وكلمة مرور. ولا يظهر الزر إلا بعد ضبط المفاتيح، فلا يصل المريض إلى طريق مسدود.

**الخطوة ١ — أنشئ بيانات الاعتماد** من
[console.cloud.google.com](https://console.cloud.google.com):

1. **APIs & Services ← OAuth consent screen** ← External ← اكتب اسم التطبيق
   وبريد الدعم والنطاق. (انشره، وإلا لن يدخل سوى المستخدمين التجريبيين.)
2. **APIs & Services ← Credentials ← Create credentials ← OAuth client ID**
   ← نوع التطبيق: **Web application**.
3. تحت **Authorised redirect URIs** أضف هذا الرابط حرفياً:
   ```
   https://smartmedcon.com/api/auth/google/callback
   ```
   وعدم تطابقه هو السبب الأشهر لرفض Google تسجيل الدخول — يجب أن يكون `https`
   وبلا شرطة مائلة في النهاية وبنفس المسار تماماً.
4. انسخ **Client ID** و **Client secret**.

**الخطوة ٢ — hPanel ← Web App ← متغيرات البيئة**، ثم أعد التشغيل:

```
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
```

وللإيقاف لاحقاً احذف المتغيّرين وأعد التشغيل — يختفي الزر ويبقى الدخول باسم
المستخدم وكلمة المرور كما هو.

### كيف تُربط الحسابات

- الدخول بنفس حساب Google يعود دائماً إلى نفس ملف المريض.
- إذا كان المريض **مسجّلاً سابقاً بكلمة مرور** ثم دخل عبر Google بنفس البريد،
  فسيدخل إلى **ملفه الحالي** لا إلى ملف مكرّر، وتبقى كلمة مروره تعمل.
- ويحدث ذلك فقط عندما تؤكّد Google أن البريد موثّق. أما البريد غير الموثّق فلا
  يُربط أبداً، لأن ذلك يتيح لشخص الوصول إلى ملف طبي بادعاء بريد لا يملكه.

## ٨. أفاتار المقابلة

صفحة المقابلة (`/consultation/<id>/avatar`) تُشغّل الطبيب الذكي الذي يأخذ التاريخ
المرضي. له شكلان ممكنان — **والأسئلة الطبية متطابقة تماماً في الحالتين**، الفرق
في العرض فقط.

### الوضع الحالي: الصوت فقط (مجاني) ✅

يرى المريض صورة طبيب العيادة ويسمع الأسئلة بصوت المتصفح. تظهر أشرطة صوتية
متحرّكة أثناء حديث الذكاء الاصطناعي، ونقطة خضراء عندما يحين دور المريض.

**لا يكلّف شيئاً لكل استشارة.** وهذا اختيار مقصود: الأفاتار المرئي المدفوع
يُحاسب بالدقيقة، وجلسة أخذ تاريخ مرضي كاملة قد تكلّف أكثر من عائد الاستشارة ($5).

**لتغيير صورة الطبيب:** استبدل الملف `client/public/doctor-avatar.webp` ثم ارفع
التغيير إلى GitHub. أبقِ الحجم صغيراً — الملف الحالي 68 كيلوبايت؛ وأي حجم يتجاوز
بضع مئات من الكيلوبايت سيبطئ الصفحة على بيانات الجوال. وإذا اختفى الملف تظهر
أيقونة سمّاعة الطبيب بدل أن تتعطّل الصفحة.

### التحدّث بدل الكتابة

يستطيع المريض أن **يُجري محادثة صوتية** مع الأفاتار. يضغط زر المايكروفون مرة
واحدة، ثم تصبح المحادثة بدون استخدام اليدين: يتكلّم، فيجيبه الأفاتار صوتياً، ثم
يُفتح المايكروفون تلقائياً لإجابته التالية. والضغط مرة أخرى يوقف ذلك.

- تظهر موجة صوتية والكلمات على الشاشة ليطمئن أنه مسموع.
- يُغلق المايكروفون أثناء حديث الأفاتار، فلا يلتقط صوت الأفاتار نفسه ويعيد
  إرساله كأنه كلام المريض.
- **الكتابة تبقى متاحة دائماً** — لا شيء إجباري.
- التعرّف على الكلام يحتاج **Chrome أو Edge أو Safari**. وعلى Firefox يُخفى زر
  المايكروفون تلقائياً ويكتب المريض فقط.
- التعرّف يتبع لغة المقابلة، فيُفرَّغ الكلام العربي كعربية.

### اللغة: عربي أم إنجليزي

**الأفاتار يتبع اللغة التي يكتب بها المريض فعلياً.** إذا كتب بالعربية، تتحوّل
الأسئلة والصوت إلى العربية — حتى لو أُنشئت الاستشارة بالإنجليزية. كتابة المريض
تتقدّم دائماً على الإعداد المحفوظ.

- الزائر الجديد يبدأ بلغة **متصفّحه**، ومع غياب الإشارة يبدأ بـ**العربية**.
- الكتابة المختلطة تبقى عربية — «عندي ألم وعملت CT scan» تُعامل كعربية، لأن
  المتحدثين بالعربية يخلطون عادةً مصطلحات طبية إنجليزية.
- زرّا AR / EN في الأعلى يتيحان التبديل يدوياً في أي وقت.

**إذا لم يكن في جهاز المريض صوت عربي** (شائع على ويندوز، ومتوفّر على الهواتف)
يظهر تنبيه كهرماني يوضح أن النص المعروض صحيح ويمكنه القراءة بدل الاستماع. لا
يتعطّل شيء — التأثير على الصوت فقط.

### اختياري: الأفاتار المرئي المدفوع (مُعطّل حالياً)

تكامل LiveAvatar/HeyGen مبنيّ وجاهز لكنه خامل. وما دامت المتغيّرات أدناه غير
مضبوطة، فإن زر "ابدأ الطبيب المرئي" لا يظهر أصلاً، فلا يمكن تشغيله أو تحميلك
تكلفة عن طريق الخطأ.

للتفعيل، اضبط التالي في hPanel ← Web App ← متغيرات البيئة ثم أعد التشغيل:

| المتغيّر | من أين تحصل عليه |
|---|---|
| `LIVEAVATAR_API_KEY` | liveavatar.com ← الإعدادات ← مفاتيح API |
| `LIVEAVATAR_AVATAR_ID` | liveavatar.com ← Avatars ← انسخ الـ UUID |
| `LIVEAVATAR_SANDBOX` | `false` — الوضع التجريبي يرفض الأفاتارات الجاهزة (مُختبَر) |

⚠️ **راجع سعر الدقيقة مقابل سعر الاستشارة ($5) أولاً.**

**المريض لا يرى أي خطأ:** إذا كان المفتاح خاطئاً أو الخدمة متوقفة، تنتقل الجلسة
بهدوء إلى الوضع الصوتي بدل أن تتعطّل. ويُكتب السبب في وحدة تحكّم المتصفح وفي
hPanel ← Runtime logs لتتمكّن من تشخيصه.

### أين يقرأ الطبيب النتيجة

كل ما يقوله المريض للأفاتار يظهر في **لوحة الإدارة ← مراجعة الذكاء الاصطناعي**
تحت عنوان *"محادثة المريض مع الأفاتار الطبي"*، فوق المستندات المرفوعة. راجعها
قبل اعتماد التقارير.
